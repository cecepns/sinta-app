# API (backend)
# Optional: ganti hanya jika API bukan bawaan produksi
# Lokal: VITE_API_URL=http://127.0.0.1:4000
# VITE_API_URL=

# Public URL app — untuk kode QR sub menu (override host saat perlu, mis. deploy ke domain lain)
# Jika kosong, dipakai window.location.origin (lokal: http://localhost:5173)
# VITE_APP_BASE_URL=
